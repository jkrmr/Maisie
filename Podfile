# frozen_string_literal: true

platform :ios, "9.0"

target "Maisie" do
  use_frameworks!

  pod "Gloss", "~> 1.2"
  pod "PromiseKit", "~> 4.0"
  pod "Kingfisher", "~> 3.0"

  target "MaisieTests" do
    inherit! :search_paths
  end
end
